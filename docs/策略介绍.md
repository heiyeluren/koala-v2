⏺ Koala 支持的算法模式

  一、访问控制（Access Control）
  ┌────────┬──────────────────────┬────────────────────────┐
  │  类型  │       配置位置       │          说明          │
  ├────────┼──────────────────────┼────────────────────────┤
  │ 白名单 │ [[access.whitelist]] │ 匹配即放行，优先级最高 │
  ├────────┼──────────────────────┼────────────────────────┤
  │ 黑名单 │ [[access.blacklist]] │ 匹配即拦截             │
  └────────┴──────────────────────┴────────────────────────┘
  内部使用 Direct 算法，不计数，命中即返回结果。

  典型场景：
  - VIP用户直接放行
  - 内网IP白名单
  - 封禁恶意用户/IP/设备

  ---
  二、限流算法（Rate Limiting）
  ┌────────────┬──────────┬─────────────────────┐
  │  配置类型  │ 内部算法 │      适用场景       │
  ├────────────┼──────────┼─────────────────────┤
  │ count      │ Count    │ 固定时间窗口计数    │
  ├────────────┼──────────┼─────────────────────┤
  │ freq       │ Leak     │ 滑动窗口/平滑限流   │
  ├────────────┼──────────┼─────────────────────┤
  │ accumulate │ Base     │ 累积阈值 + 二级限流 │
  └────────────┴──────────┴─────────────────────┘
  1. count - 固定窗口计数

  时间窗口内累计 N 次后拒绝，窗口结束后重置

  配置示例：
  [[rules.business]]
  name = "login_limit"
  type = "count"
  match = { act = "login", uid = "+" }
  limit = { time = "60s", count = 5 }    # 60秒内最多5次
  result = "login_limit"

  适用场景：
  - 每天最多发帖 10 次
  - 每分钟最多登录 5 次
  - 每小时最多评论 20 次

  ---
  2. freq - 滑动窗口/漏桶

  任意连续 N 秒内最多 M 次，无边界突发问题

  配置示例：
  [[rules.advanced]]
  name = "api_smooth_limit"
  type = "freq"
  match = { act = "api_call", uid = "+" }
  limit = { time = "60s", count = 100 }   # 任意60秒内最多100次
  result = "api_limit"

  适用场景：
  - API 调用平滑限流
  - 搜索请求限制
  - 需要精确控制流量的场景

  ---
  3. accumulate - 累积阈值 + 二级限流

  总量达到阈值前无限制，达到后启用二级限流

  配置示例：
  [[rules.advanced]]
  name = "ask_base_control"
  type = "accumulate"
  match = { act = "ask", ip = "+" }
  limit = { base = 20, time = "10s", count = 1 }  # 总量超过20后，每10秒限1次
  result = "deny"

  适用场景：
  - 正常用户快速操作，高频用户受限
  - "每天超过 20 次后，每 10 秒限 1 次"
  - 渐进式限流

  ---
  三、规则优先级

  白名单 > 黑名单 > Business > Post > Advanced > Default

  ---
  四、匹配模式汇总
  ┌──────────┬────────────────┬──────────────────┐
  │   语法   │      示例      │       说明       │
  ├──────────┼────────────────┼──────────────────┤
  │ 精确匹配 │ "login"        │ 值等于 "login"   │
  ├──────────┼────────────────┼──────────────────┤
  │ 任意非空 │ "+"            │ 匹配任何非空值   │
  ├──────────┼────────────────┼──────────────────┤
  │ 取反     │ "!admin"       │ 值不等于 "admin" │
  ├──────────┼────────────────┼──────────────────┤
  │ 多值     │ "post,comment" │ 值为其中之一     │
  ├──────────┼────────────────┼──────────────────┤
  │ 范围     │ "1-100"        │ 数值在范围内     │
  ├──────────┼────────────────┼──────────────────┤
  │ 大于     │ ">1000"        │ 数值大于阈值     │
  ├──────────┼────────────────┼──────────────────┤
  │ 小于     │ "<100"         │ 数值小于阈值     │
  ├──────────┼────────────────┼──────────────────┤
  │ IP通配   │ "192.168.*.*"  │ IP匹配通配符     │
  ├──────────┼────────────────┼──────────────────┤
  │ 字典引用 │ "@whitelist"   │ 值在字典文件中   │
  └──────────┴────────────────┴──────────────────┘
  ---
  五、算法选择指南
  ┌────────────────────────────┬──────────────┐
  │            需求            │   推荐算法   │
  ├────────────────────────────┼──────────────┤
  │ 每天/每小时固定次数限制    │ count        │
  ├────────────────────────────┼──────────────┤
  │ API 限流、需要平滑控制     │ freq         │
  ├────────────────────────────┼──────────────┤
  │ 正常用户不限、高频用户限速 │ accumulate   │
  ├────────────────────────────┼──────────────┤
  │ 黑白名单                   │ 访问控制规则 │
  └────────────────────────────┴──────────────┘ 